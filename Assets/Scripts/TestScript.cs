using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestScript : MonoBehaviour
{
    //note that instantiate should be set in the y axis slightly above the background so it can't collide with the background
    /*int tiedObs = 0;

    void Start()
    {
        StartCoroutine(SimplePause());
    }

    void setObs(int pCount) {
        tiedObs = pCount;
    }

    IEnumerator SimplePause()
    {
        yield return new WaitForSeconds(1);
        //gameObject.isTrigger = false;
        //move gameobject completely down to the background

    }

    void OnTriggerEnter(Collider other) {
        if (other.tag == "Collision") {
            Destroy(gameObject);
        } 
    }*/

}
